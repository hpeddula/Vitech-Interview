import React from 'react';

const TotalCounter =(props) => {
    return(
        <div>
            <p>This is a new Total Counter {props.total}</p>
        </div>
    );

}

export default TotalCounter;